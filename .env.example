# Microspeed no necesita variables para correr el modo online: usa PeerJS con
# sus defaults (broker + STUN/TURN gratuitos de la librería), igual que
# PlayHubGX. Este archivo queda como plantilla por si en el futuro se añade
# algún flag de build (p. ej. endpoint de telemetría, feature flags, etc.).
